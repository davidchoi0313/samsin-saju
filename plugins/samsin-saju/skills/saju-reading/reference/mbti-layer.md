# MBTI 개인화 레이어 계약 (mbti-layer.md)

> 본 파일은 삼신이 사주풀이 플러그인의 **MBTI 개인화 레이어 계약을 한곳에 모은 단일 소스**입니다.
> `brand.md`가 브랜드 문자열을 중앙화하듯, 이 파일은 MBTI 레이어의 스키마·분리 규칙·가드·면책을 중앙화합니다.
> SKILL.md·조력자(`agents/*.md`)·파일럿 메뉴(`menus/07·08·12`)의 모든 MBTI 언급은 이 파일의 계약을 따릅니다.
> MBTI 레이어를 끄거나 규칙을 바꿀 때는 **이 파일 1곳만** 손보면 전체가 일괄 정합됩니다.
> 상태: 프로토타입(Phase 0 파일럿 — 07 직업운·08 결혼연애운·12 오늘의운세). 전략팀 종합계획 §6 스펙 정합.

## 0. 한 줄 정체성

MBTI는 **사주 사실이 아니라 사용자가 스스로 신고한 자기보고 참고값**이며, 삼신이 풀이의 **톤·조언 전달을 개인화하는 데에만** 쓰입니다. 사주팔자·오행·십신·신살·대운은 오직 만세력 엔진 산출 카드에서만 오고, MBTI는 그 근거를 한 글자도 건드리지 않습니다.

## 1. 계층 배치 — 엔진 카드와 분리된 제3의 자기보고 레이어

삼신이는 2층 구조입니다: [1층 만세력 엔진 → 결정론 산출 카드] → [2층 AI 해석]. MBTI는 이 **어느 쪽에도 섞이지 않는 제3의 별도 레이어**입니다.

```
[1층 만세력 엔진]  생년월일·시·성별 → saju_engine.py → 산출 카드(JSON)
        │   사주팔자·오행·십신·대운·세운·신살·용신 = 결정론 산출 (사주 사실의 유일 근거)
        │
[별도] MBTI 자기보고 레이어  사용자 입력(선택) → mbti_profile(JSON)  ← 엔진 산출 아님. 사주 사실 근거 아님.
        │   16유형 4글자 코드(E/I·S/N·T/F·J/P) = 사용자가 스스로 신고한 값
        ▼
[2층 AI 해석]  ── 사주 사실 = 엔진 카드 필드값만 근거 ──────────
        │        ── 톤·조언 개인화 = MBTI 레이어 참조(사실 아님) ──
        ▼   삼신이 한 목소리로 감싸 전달 + 면책 2단(+ MBTI 추가 단서)
```

- MBTI 레이어는 엔진 카드(JSON)에 **병합하지 않습니다.** 별도 객체 `mbti_profile`로만 존재합니다.
- MBTI가 엔진 카드 필드값을 덮어쓰거나 보정하는 경로는 **존재하지 않습니다.**

## 2. 스키마 (§6.2 정합 — 엔진 카드와 별도 JSON 객체)

MBTI 프로파일은 엔진 산출 카드와 **별도 JSON 객체**로 관리합니다(엔진 카드에 병합 금지).

```json
{
  "mbti_profile": {
    "provided": true,
    "type": "INTJ",
    "axes": { "EI": "I", "SN": "N", "TF": "T", "JP": "J" },
    "axis_strength": null,
    "source": "self_report",
    "note": "self-reported. NOT a saju fact. tone/advice personalization only."
  }
}
```

| 필드 | 뜻 | 규칙 |
|---|---|---|
| `provided` | MBTI 제공 여부 | `false`이거나 객체 자체가 없으면 → 해석 레이어는 MBTI를 **무시**, 기존 풀이 그대로(강등·오염 없음) |
| `type` | 16유형 4글자 코드 | 대문자 4글자(예: `INTJ`). 유효하지 않으면 `provided:false`로 처리 |
| `axes` | 4축 분해 | EI(E/I)·SN(S/N)·TF(T/F)·JP(J/P). 톤·조언 힌트 도출에 참조 |
| `axis_strength` | 축 강도(슬라이더) | **v2 후보.** MVP는 항상 `null`(4축 코드만) |
| `source` | 출처 | 항상 `self_report` **고정** — 엔진 산출 아님을 스키마 레벨에서 못박음 |
| `note` | 성격 메모 | "사주 사실 아님·톤/조언 개인화 전용"을 스키마 안에 명시 |

- **유효성 판정:** `type`이 16유형(E/I·S/N·T/F·J/P 조합) 중 하나가 아니면 `provided:false`로 강등하고 MBTI를 쓰지 않습니다. 파싱·검증은 엔진과 **무접점**인 별도 유틸(`scripts/mbti_util.py`, 선택)에서 하며, 결정론 엔진(`saju_engine.py`)은 절대 손대지 않습니다.

## 3. 분리 규칙 — 해석 레이어의 MBTI 사용 계약 (§3.2 정합)

### 3.1 MBTI 레이어가 **할 수 있는 것** (톤·조언 개인화만)

- 조언의 **전달 방식** 조정: 계획 선호(J) 신호면 처방을 단계·체크리스트 결로, 즉흥 선호(P) 신호면 "마음 가는 것부터" 결로 표현.
- 위로/축하의 **톤** 조정: 내향(I) 신호면 "혼자 정리할 시간"을 곁들이는 식.
- **어느 조언을 먼저 꺼낼지 우선순위**만 조정.

핵심: MBTI는 이미 엔진 카드로 완성된 해석 골격의 **말투와 순서만** 다듬습니다. 골격의 사주 근거·필드 앵커는 불변입니다.

### 3.2 MBTI 레이어가 **절대 할 수 없는 것** (HARD-LOCK)

- MBTI를 **사주 사실의 근거로 인용 금지.** "네 사주가 이러니 너는 INTJ"(사주→MBTI 도출) 금지. "너는 INTJ니까 사주에 관성이 강하다"(MBTI→사주 위장) 금지.
- MBTI로 **엔진 카드 필드값(오행·십신·신살·대운)을 덮어쓰거나 보정 금지.** 사주 사실은 오직 엔진 카드에서만.
- MBTI를 **과학적 사실로 단정 금지** — "재미·자기이해 참고" 톤 유지. 유형 낙인·고정관념·우열 서술 금지("T라서 공감을 못 한다"류 금지). 유형은 우열이 아니라 결의 차이.
- **네 글자 처방의 '└ 근거'(필드 앵커)는 여전히 엔진 카드 필드만.** MBTI는 처방의 앵커가 될 수 없고, 앵커된 처방의 '말투'만 다듬습니다.

## 4. 4대 가드 (코드/프롬프트 레벨 강제 — §6.3 정합)

MBTI가 사주 근거로 새는 경로를 원천 차단하는 4개 가드입니다. 이 중 하나라도 위반하면 분리 위반이며, 검증 루브릭상 즉시 실패(0점)입니다.

1. **가드 1 — 사주 근거 문장에 MBTI 등장 차단.** MBTI 문자열(유형 코드·축 이름)이 "사주/오행/십신/신살/대운/용신"을 근거로 대는 문장에 등장하면 차단.
2. **가드 2 — 사주→MBTI 도출 차단.** "사주에 따르면 너는 [MBTI]" 패턴 금지(엔진 카드에서 MBTI를 유도하는 경로 자체를 두지 않음).
3. **가드 3 — 처방 근거 줄에 MBTI 앵커 금지.** 네 글자 처방의 "└ 근거" 줄에 MBTI가 앵커로 등장 금지 — 근거는 엔진 카드 필드만.
4. **가드 4 — 미제공 시 언급 금지.** `mbti_profile.provided != true`(또는 객체 부재)이면 MBTI를 **언급조차 하지 않습니다.** 없는 MBTI를 짐작·추정해 넣는 것도 금지.

## 5. 신설 면책 문구 (MBTI 추가 단서 — §5.1 정합)

파일럿 메뉴에서 MBTI를 실제로 반영한 풀이는, 기존 면책 2단(SKILL.md §5.0)의 **2단(정식 면책) 뒤에 아래 추가 단서를** 붙입니다. 기존 공통 면책을 약화하지 않습니다.

> MBTI는 자기 이해를 돕는 성격 유형 참고 도구이며, 과학적 진단이나 확정된 사실이 아닙니다. 본 풀이에서 MBTI는 사주 해석의 근거가 아니라, 조언을 더 편히 전하기 위한 참고로만 쓰였습니다. 유형은 우열이 아니라 결의 차이이며, 참고로만 품어 주세요.

- **부착 조건:** `mbti_profile.provided == true`이고 풀이에 MBTI 톤·조언을 실제로 얹었을 때만 부착합니다(가드 4 정합 — 미제공 시 이 단서도 붙지 않음).
- **부착 위치:** §5.0 2단 정식 면책 원문 뒤. 해당 메뉴에 다른 §5.1 추가 단서(연애·궁합 % 단서 등)가 있으면 그와 함께 나열합니다.

## 6. 데이터 흐름에서의 위치 (§3.3 정합)

1. **입력 수집**(SKILL §2) 단계에서 사주 필수 입력(성별·음양력·생년월일·생시)을 다 받은 **뒤**, MBTI를 **선택 입력**으로 가볍게 여쭙니다(삼신이 보이스, SKILL §2.3). 모르면 바로 넘어가고 강등 없습니다.
2. **엔진 호출**(SKILL §3)로 산출 카드 확보 — 이 단계는 MBTI와 무관하게 그대로.
3. **내부 조력자 호출**(SKILL §4.0) 시 산출 카드 + (있으면)`mbti_profile`을 함께 넘깁니다. 조력자(만세 할아버지/별이 할멈)는 **카드 필드로 해석 골격을 먼저 완성**한 뒤, `provided==true`면 **처방·조언의 "말투와 우선순위"만** MBTI 힌트로 다듬습니다.
4. **삼신이가 감싸 전달** + 면책 2단(+ §5 MBTI 추가 단서).

## 7. 파일럿 범위·확장 (§4·§6.4 정합)

- **Phase 0 파일럿:** 07 직업운(그룹 A)·08 결혼연애운(그룹 A)·12 오늘의운세(그룹 B). 이 3개만 이번 프로토타입 대상입니다.
- **파일럿 외 메뉴는 이번엔 미적용.** 파일럿 3개 외 나머지 메뉴 가이드는 손대지 않습니다(스코프 고정).
- **채용참고(15)는 원칙적 제외.** MBTI 기반 유형 스크리닝은 채용 차별 위험을 가중하므로 이번 파일럿·MBTI 결합 대상이 아닙니다(§4.3 원칙). 이 예외는 담당자님·법무 승인 없이 열지 않습니다.
- **12(오늘의운세)는 짧고 가벼운 정체성 유지.** MBTI 반영도 톤 한 스푼까지만, 풀세트 처방 금지(가벼움이 정체성).

## 8. 거버넌스 위반 0 확인 문장

> 본 계약은 삼신이 SKILL.md §0(엔진 산출만이 사주 근거) 위반 0, §7.5(단일 창구·새 user-facing 스킬 금지·페르소나 일관) 위반 0을 만족한다. MBTI는 엔진 산출 카드와 물리적으로 분리된 자기보고 입력 레이어(`mbti_profile`)로만 존재하고, 사주 사실 근거로는 절대 쓰이지 않으며 오직 2층 해석의 톤·조언 개인화 힌트로만 참조된다. MBTI는 새로운 user-facing 스킬이 아니라 삼신이(saju-reading)가 내부에서 참조하는 모듈이며, 사용자 대면 화자는 여전히 삼신이 하나다. 엔진 카드 필드값을 MBTI가 덮어쓰거나 보정하는 경로는 존재하지 않는다.

> 근거:
> - 내용: MBTI 개인화 레이어 계약 — 엔진 카드와 분리된 자기보고 레이어, 톤·조언 개인화 전용, 사주 사실 근거 절대 불가. 4대 가드로 분리 위반 원천 차단. 프로토타입 스펙(전략팀 종합계획 §3·§5.1·§6)을 개발 구현 계약으로 중앙화.
> - 출처 ID: samsin-mbti-layer-contract-2026-07-07#docs/MBTI결합-종합계획.md§3·§6
> - 마지막 확인: 2026-07-07
> - 다음 확인: 2026-10-07
> - 상태: 적용 중 (프로토타입 Phase 0)
